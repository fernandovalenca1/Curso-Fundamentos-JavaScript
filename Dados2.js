let preco = 19.90;
let desconto = 0.4;
let precoComDesconto = preco * (1 - desconto)

console.log(preco * (1 - desconto));
console.log(precoComDesconto);

let nome = "caderno e ";
let categoria = "papelaria";
console.log (nome + categoria); // string nada mais é do que uma senquencia de simbolos ( texto).
